# A varaable can have a short name (like x and y) or a more descriptive name (age, car name, total_volume).
myvar = "John"    # Varaiable names must start with a letter or the underscore character.
my_var = "John"
_my_var = "John"
myVar = "John"    # Variables names are case-sensitive (age, Age and AGE are three differnt variables)
MYVAR = "John"
myvar2 = "John"   # A variable name can only contain alpha-numeric characters and underscore (A-z, 0-9, and _)

2myvar = "John"   # A variable name cannot start with a number
my-var = "John"
my var = "John"

# Each word, except the first, starts with a capital letter
myVariableName = "John"   # Camel Case
# Each word stars with a capital letter
MyVariableName = "John"   # Pascal Case
# Each word is separated with an underscore character
my_variable_name = "John"
